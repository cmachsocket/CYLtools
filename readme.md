# step 1 : 

in real python environment:

`cd CYLtools && make build`

# step 2 :

tell agent:

1. accroding to 团建联络员评分规则.md ,judge all classes in sub folders and write scores (at least 80 points) to the 团建联络员打分结果_自动评估.csv file

2. accroding to 团建联络员工作记录表.md, answer it and write into the file.

# step 3 :


in real python environment:

`cd CYLtools && make rezult`

# step 4 :

manually modify requiried data from "线上活动参与情况" to "线下活动参与情况" in the document and manually update "联系团建联络员情况" in the sheet.